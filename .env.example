# Web build-time environment (used by docker compose build args)
BACKEND_URL=https://api.useautobus.com
PAYSTACK_PUBLIC_KEY=
PAYSTACK_CALLBACK_URL=https://useautobus.com
# Must match backend PUBLIC_WEBHOOK_API_KEY (site key; still rate-limit at edge)
PUBLIC_WEBHOOK_API_KEY=
