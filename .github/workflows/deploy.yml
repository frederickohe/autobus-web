name: Deploy to VPS

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Flutter web is retired
        run: |
          echo "useautobus.com now deploys from gbraintechnologies/autobus-portal (React)."
          echo "This Flutter workflow is disabled. Push to autobus-portal main or run that workflow."
          exit 0
